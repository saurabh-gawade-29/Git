**Basic Git Commands**
git init
git status
git add .
git commit -m "message"
git log --oneline

**Ignore Files and Folder**
create .gitignore file in root dir and add file and folders name in it.

**IMP**
git cannot ignore empty folders.
create .gitkeep file inside that folder so you can track that empty folder

** Branching **
git branch <branch-name> : create new branch
git switch <branch-name> : Switch to branches
git branch -d <branch-name> : delete branch
git checkout -b <branch-name>
** Merging **
git merge <branch-name> : merge feature branch to master/main - it will create new commit automatically
-- resolve merge coflicts by removing markers

** Stashing **
-- temp. store point

git stash : temp store and working tree clean
git stash list : list all the stashes
git stash pop / git stash apply :  unstash

** git tags **
git tag -a <tag-name>-m "<message>" -- annoted tag
git tag <tag-name> - lightweight tag


** git rebase **
to avoid merge commit we are using git rebase
1. First we need to create feature branch with some changes and commit
2. git rebase <branch-name> -- master/main
3. check commit history using git log -- oneline
4. then go to master and now merge this branch to <branch-name> -- master/main
5. and now again check commit history no merge commit is there

** Extra **
git branch -M <new-branch> -- change branch name
git pull -- pull from origin
git push -- push to the origin
git fetch -- fetch branch names and changes from origin